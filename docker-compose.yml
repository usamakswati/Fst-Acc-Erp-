version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: fastaccounts-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: erp_user
      POSTGRES_PASSWORD: erp_password
      POSTGRES_DB: fastaccounts_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
